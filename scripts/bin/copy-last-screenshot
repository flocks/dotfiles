 #!/bin/bash

last_screenshot=$(ls "$HOME/screenshots" -1th | head -n 1 | awk -v home="$HOME/screenshots/" '{print home $1}')
xclip -selection clipboard -target image/png "$last_screenshot"
notify-send "COPIED" "$FILENAME copied directly on clipboard"



