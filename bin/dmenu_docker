#!/bin/bash

font="Liberation Sans-15:Bold"

choice=$(docker ps | tail -n +2 |  awk '{print $2}' | dmenu -fn "$font" -l 10)
id=$(docker ps | grep "$choice" | awk '{print $1}')
echo $id
echo "$id" | xclip -selection c
