#!/bin/bash

BOOKMARK_FILE=~/.bookmarks
set -e
url=$(dmenu -i -p "BOOKMARK" -l 10 < $BOOKMARK_FILE)
if grep -q "$url" $BOOKMARK_FILE; then
    google-chrome-stable "$url"
else
    google-chrome-stable "https://www.google.com/search?&q=$url"
  fi


