#!/bin/bash

if lsusb | grep -q "USB Keyboard"
then
  echo "resetting keys remap"
  setxkbmap -option
else
  echo "xmodmap"
  xmodmap ~/.xmodmap.conf
fi
xset r rate 150 200
setxkbmap -layout us -variant mac
